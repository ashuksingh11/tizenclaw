Implement platform plugin metadata loading

Add metadata-driven shared library discovery to libtizenclaw-core.
PlatformContext now retains discovered plugins, works without plugins,
and reports capabilities from loaded plugin metadata.

Export claw_plugin_info and claw_plugin_free_string from the
standalone Tizen metadata plugin and add discovery metadata to the
CLI, skill, and LLM backend metadata plugin crates.

Refresh the host build flow to compile the reference metadata plugin,
update Cargo.lock, and record the full six-stage workflow in DASHBOARD.
