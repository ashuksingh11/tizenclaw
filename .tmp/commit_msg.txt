Implement runtime permission policy model

Rebuild the tclaw-runtime permission stack with deterministic
policy evaluation, explained decisions, and prompt mediation.

Add config-driven override rules, tool minimum permission
levels, sandbox-aware inputs, and shell validation enforcement.

Thread richer permission state through runtime config,
conversation tool definitions, session history, and exports.
