Standardize runtime layout under .tizenclaw roots

Move mutable host and Tizen runtime state to the canonical .tizenclaw
roots while keeping packaged assets under /opt/usr/share/tizenclaw.
Align services, deploy scripts, container helpers, path resolution, and
CLI documentation with the new contract.
