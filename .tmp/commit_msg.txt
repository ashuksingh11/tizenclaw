Refine daemon startup and shutdown flow

Implement the requested 7-phase daemon boot sequence and
daemon option parsing in the Rust entry point.

Add the append-only boot phase logger, devel-mode prompt
runner, and a stoppable mDNS scanner so graceful shutdown
can complete after readiness.

Update the IPC contract scenario and record the host build,
runtime checks, devel verification, and host test results in
the development dashboard.
