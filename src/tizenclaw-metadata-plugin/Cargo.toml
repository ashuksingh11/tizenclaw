[package]
name = "tizenclaw-metadata-plugin"
version = "0.1.0"
edition = "2021"
publish = false

[features]
default = ["platform-plugin-exports"]
helper-only = []
platform-plugin-exports = []

[lib]
name = "tizenclaw_plugin"
crate-type = ["cdylib", "rlib"]

[dependencies]
serde_json = "1"
