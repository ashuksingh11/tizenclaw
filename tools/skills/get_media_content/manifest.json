{
    "name": "get_media_content",
    "risk_level": "low",
    "description": "Search and list media files (images, videos, audio, documents) stored on the device. Can filter by media type.",
    "parameters": {
        "type": "object",
        "properties": {
            "media_type": {
                "type": "string",
                "description": "Filter by type: 'image', 'video', 'sound', 'music', or 'all' (default: 'all')",
                "enum": ["all", "image", "video", "sound", "music"]
            },
            "max_count": {
                "type": "integer",
                "description": "Maximum number of items to return (default: 20)"
            }
        },
        "required": []
    }
}
