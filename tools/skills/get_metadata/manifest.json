{
    "name": "get_metadata",
    "risk_level": "low",
    "description": "Extract metadata from a media file (audio/video). Returns title, artist, album, genre, duration, bitrate, sample rate, and more.",
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "Absolute path to the media file"
            }
        },
        "required": ["file_path"]
    }
}
