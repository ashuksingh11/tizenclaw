{
    "name": "schedule_alarm",
    "version": "2.0",
    "description": "Schedules an alarm to launch a specific application at a given time using Tizen Alarm API with app_control.",
    "entrypoint": "python3 schedule_alarm.py",
    "risk_level": "high",
    "dependencies": [],
    "parameters": {
        "type": "object",
        "properties": {
            "app_id": {
                "type": "string",
                "description": "Target application ID to launch when the alarm fires (e.g. org.tizen.setting)"
            },
            "datetime": {
                "type": "string",
                "description": "Target time in ISO format (YYYY-MM-DDTHH:MM:SS)"
            }
        },
        "required": [
            "app_id",
            "datetime"
        ]
    }
}