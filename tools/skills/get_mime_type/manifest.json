{
    "name": "get_mime_type",
    "category": "Media",
    "risk_level": "low",
    "description": "Get MIME type from a file extension or file path, or get file extensions for a given MIME type.",
    "parameters": {
        "type": "object",
        "properties": {
            "file_extension": {
                "type": "string",
                "description": "File extension to look up (e.g. 'mp4', 'jpg', 'pdf')"
            },
            "file_path": {
                "type": "string",
                "description": "File path to detect MIME type from"
            },
            "mime_type": {
                "type": "string",
                "description": "MIME type to get file extensions for (e.g. 'image/jpeg')"
            }
        },
        "required": []
    }
}
