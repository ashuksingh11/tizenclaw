Resolve dead code warnings in tizenclaw-core library

Why:
The unused structures within the internal library triggered native
dead-code compiler warnings during GBS tokenization phases.

What:
- Expose internal lib modules via robust public API boundaries
- Assign pub modifiers across unused FFI layouts dynamically
- Eliminate warnings verifying multi-architecture targets natively
