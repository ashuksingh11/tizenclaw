# Bundled crun Source

We also support building `crun` from source during RPM build.

Expected source path:

- `third_party/crun/src/crun-1.26.tar.gz`

Use `scripts/fetch_crun_source.sh` to fetch the source tarball.
